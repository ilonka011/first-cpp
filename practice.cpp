#include <iostream>
using namespace std;


void sum(int x, int y, int z, int a) {
    std::cout << x + y + z + a << std::endl;
}
int main() {
    sum(11, 350, 999, 14);
    std::cin >> 'What is your name?' >> std::endl;
    return 0;
}
